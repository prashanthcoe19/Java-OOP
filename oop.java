public class oop {
    int x = 5;

    // oop class is already created so we can use this to create objects
    public static void main(String[] args) {
        // here we have created an object of class oop
        oop myObj = new oop();
        myObj.x = 50;
        System.out.println(myObj.x);
    }
}
